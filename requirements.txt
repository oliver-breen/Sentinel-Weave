# ── Core runtime dependencies ─────────────────────────────────────────────────
# Required by sentinel_weave.secure_reporter (AES-GCM encryption of reports)
cryptography>=41.0.0

# ── Dashboard dependencies ─────────────────────────────────────────────────────
# Required by dashboard/app.py
flask>=3.0

# Required by dashboard/app.py (instantiated eagerly at app startup)
capstone>=5.0
yara-python>=4.3
scikit-learn>=1.4
pandas>=2.0

# ── Optional / feature-specific extras ────────────────────────────────────────
# Uncomment the groups you need:

# Red-team / offensive toolkit (BinaryFuzzer in sentinel_weave.advanced_offensive)
# pwntools>=4.12

# Memory forensics (MemoryForensicsScanner in sentinel_weave.advanced_offensive)
# volatility3>=2.7

# Azure integrations (sentinel_weave.azure_integration)
# azure-storage-blob>=12.19
# azure-ai-textanalytics>=5.3
# azure-monitor-opentelemetry>=1.3
